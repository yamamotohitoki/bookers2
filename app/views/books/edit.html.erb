<h1>編集画面</h1>

<%= form_with model: @book do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>本文</h4>
  <%= f.text_area :body %>

  <%= f.submit '保存' %>

<% end %>

<%= link_to "show",books_path(@book.id)%>
<%= link_to "Back",user_path(current_user.id) %>
